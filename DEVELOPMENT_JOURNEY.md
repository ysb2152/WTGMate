# RouteMate 개발 기록 (Living Document)

> **이 문서는 코드와 함께 git으로 관리되는 "살아있는" 개발 기록입니다.**
> 기능이 발전할 때마다 갱신합니다. 고민한 것 · 선택 · 그 이유 · 어려웠던 점을 남겨,
> 나중에 포트폴리오/면접 자료로 바로 쓸 수 있게 하는 것이 목적입니다.
> 원본 포트폴리오 초안(Word)을 토대로, 이후 실제 개발 진행분을 현행화·통합했습니다.

- **프로젝트**: RouteMate — Smart Route Planner (자연어 일정 → 장소 추출·검증 → 경로 최적화·비교)
- **스택**: React + Vite (front) / FastAPI + Python (back) / OR-Tools, Kakao Maps·Mobility·Local, 로컬 파인튜닝 LLM(Ollama)
- **레포**: `todo-route-planner` (front/back 모노 구조)

---

## 0. 업데이트 이력 (최신순)

| 시점 | 구분 | 한 일 | 관련 |
|---|---|---|---|
| 2026-08-26 | feat | 출발시각 미입력 시 약속 기준 '추천 출발시각' 역산 | PR #6 (`feature/car-real-route`) |
| 2026-08-26 | feat | 자동차 실제 도로 경로를 지도에 표시(Kakao Mobility 좌표열 활용) | PR #6 (`feature/car-real-route`) |
| 2026-08-26 | feat | 출발시각 '현재시간 기준' 체크박스 + 미선택 시 시간계산 생략(약속 순서는 유지) | PR #6 (`feature/car-real-route`) |
| 2026-08-26 | test | 경로 계산 전수 검증 하네스(90건·정합성 168/168) + 이동수단 재최적화 버그 발견·수정 | PR #5 / `docs/verification/` |
| 2026-08-26 | feat | LLM 약속시각 자동추출(재파인튜닝) + 프론트 UX(자동채움·AI뱃지·약속시각 드롭다운·현재시각) | PR #5 |
| ~2026-08-25 | feat | 시간 제약 스케줄링(출발시각·체류시간·약속시각·도착시각·위반경고) | PR #4 |
| ~2026-08-25 | refactor | Gemini → 로컬 파인튜닝 모델(Ollama) 전환 + 세 경로 모드 목적함수 실제 차별화 | PR #3 |
| 이전 | init | 초기 구현 및 포트폴리오 초안(원본 Word 문서) | — |

> 원본 Word 문서 대비 크게 달라진 3가지: **① 모델을 Gemini에서 로컬 파인튜닝 모델로 교체**,
> **② "AI 추천 = 우선순위" 동일 비용식 문제를 실제로 해결**, **③ 시간창(약속시각) 제약을 구현**.

---

# Part A. 프로젝트 개요 · 초기 설계 (현행화 반영)

## A-1. 프로젝트가 풀려는 문제
- 일정을 입력한 뒤 방문 순서를 사용자가 직접 고민해야 하는 불편함
- 가장 가까운 곳만 잇는 경로는 일정의 **중요도**를 반영하지 못함
- AI가 뽑은 장소명이 실제 지도상의 장소와 다를 수 있음
- **AI 추천 / 최단시간 / 내 우선순위** 경로를 서로 비교하기 어려움
- 경로가 바뀌었는데 거리·시간이 이전 결과와 섞이는 상태 관리 문제

## A-2. 사용자 경험 전체 흐름
1. 출발지 검색·선택 → 2. 이동수단 선택(자동차/도보/대중교통) → 3. 자연어 일정 입력
→ 4. **로컬 LLM이 장소명·할일·우선순위·약속시각 추출** → 5. Kakao 장소 검색으로 실제 후보 검증
→ 6. 사용자가 장소 확인·수정 → 7. AI추천/최단시간/내우선순위 경로 각각 계산
→ 8. Kakao Mobility(자동차) 또는 보정계산(도보/대중교통)으로 거리·시간 산출 → 9. 지도+방문순서 비교

## A-3. 세 경로 모드의 목적함수 (핵심 설계 원칙)
초기 구현의 가장 큰 문제는 **세 경로가 모두 이동시간 중심이라 결과가 비슷해지는 것**이었다.
버튼 이름만 다르게 두는 게 아니라, 각 모드가 **실제로 다른 목적함수**를 가져야 한다고 정리했다.

| 모드 | 최우선 기준 | 의도 |
|---|---|---|
| 최단시간(shortest) | 이동시간 | 우선순위 완전 무시, 이동시간 최소화 |
| 내 우선순위(priority) | 사용자 우선순위 | 중요한 장소를 반드시 앞쪽에(계층적 최적화) |
| AI 추천(ai) | 우선순위 × 방문순번 + 이동시간 | 일정의 의미와 이동효율의 균형 |

> **원본 문서에서는 "ai와 priority가 사실상 같은 비용식이라 결과가 같다"고 지적**했는데,
> 이 부분은 이후 실제로 해결했다(→ Part B-2).

## A-4. 장소 추출과 검증의 역할 분리
자연어를 바로 좌표로 쓰는 건 위험하다("강남역"이라 해도 모델이 준 좌표가 정확하다는 보장 없음).
그래서 역할을 분리한다.

| 단계 | 담당 | 역할 |
|---|---|---|
| 1 | 로컬 LLM(구 Gemini) | 자연어에서 장소명·할일·중요도·**약속시각** 추출 |
| 2 | Kakao Local | 실제 지도상의 장소 후보 검색·좌표 확정, 사용자 확인 |

## A-5. 실패를 고려한 방어적 설계
- LLM 실패 시 Mock 데이터로 화면이 멈추지 않게 처리
- Kakao Mobility 실패 시 Haversine 거리 + 이동수단별 보정 속도로 예상값 계산(estimated 표시)
- 좌표 유효성 백엔드 검증, 오류 시 사용자 알림·loading 해제

## A-6. 프론트엔드 상태 관리
경로 결과를 모드별로 독립 저장해 "덮어쓰기/이전 결과 잔존" 문제를 막는다.
- `routeResults.ai / .shortest / .priority`, `activeRoute`, `isPriorityDirty`, `scheduleKey`, `travelMode`
- 일정 변경 → 세 결과 모두 무효화 / 우선순위만 변경 → priority만 무효화
- 같은 방문순서·같은 이동수단이면 ETA 중복 호출 방지

---

# Part B. 개발 진행 로그 — 고민 · 선택 · 이유 · 어려움

> 여기부터가 실제로 손을 대며 프로그램을 발전시킨 기록이다. 각 항목은
> **무엇이 문제였나 → 어떤 선택을 했나 → 왜 그렇게 했나 → 무엇이 어려웠나** 순으로 남긴다.

## B-1. Gemini → 로컬 파인튜닝 모델(Ollama) 전환  · PR #3

**고민.** 초기엔 장소 추출을 Gemini API에 의존했다. 외부 API는 비용·쿼터·네트워크 의존이 있고,
데모를 상시 띄우거나 오프라인에서 돌리기 어렵다.

**선택.** 소형 오픈모델 **Qwen2.5-3B-Instruct**를 **QLoRA로 파인튜닝**해, GGUF(q4_k_m)로 변환하고
**Ollama에 `routemate-parser`로 등록**해 서빙한다. Gemini는 버리지 않고 **학습 데이터 생성용 교사 모델**로만 남긴다.

**왜.**
- 추론 비용 0 · 오프라인 · 프라이버시. 태스크가 좁아서(장소 추출 JSON) 소형 모델도 파인튜닝하면 충분.
- **좌표는 LLM을 신뢰하지 않는다**: 모델은 name/task/priority만 뽑고, 좌표·주소는 항상 Kakao로 재조회해 덮어쓴다(특히 소형 모델은 좌표를 거의 항상 틀림).
- 학습을 채팅 형식이 아니라 **alpaca 프롬프트(순수 텍스트 이어쓰기)**로 하고, 추론도 `raw=True`로 같은 형식을 맞춘다.

**AI 가중치 튜닝.** AI 모드의 우선순위 지배력 상수 `AI_PRIORITY_WEIGHT_SEC`를 **300 → 100**으로 조정.
실측 스윕 결과 60 미만은 우선순위 과소반영, 100 초과는 사실상 priority 모드와 같아져서, 60~100이
"중요한 곳 먼저 가되 나머지는 이동시간 최적화"의 균형 구간이었다.

**어려웠던 점.**
- **학습/추론 프롬프트 형식이 조금만 어긋나도 성능이 급락**한다. `TASK_INSTRUCTION`을 backend와 데이터 생성 스크립트에서 byte 단위로 동일하게 유지해야 했다.
- GGUF 가중치가 1.9GB라 git에서 제외하고 Colab 노트북으로 재생성하는 체계를 만들어야 했다.
- Ollama **콜드 스타트**: 서버 기동 직후 첫 요청은 모델 로딩 때문에 실패→Mock 폴백될 수 있다(예열 후 정상).

## B-2. "AI 추천 = 우선순위" 문제의 실제 해결  · PR #3

**고민.** 원본 포트폴리오 문서가 스스로 지적한 핵심 약점 — ai와 priority가 거의 같은 비용식이라
방문 순서가 같아지는 문제. 게다가 우선순위를 이동시간에 그냥 더하기만 하면, **순서를 바꿔도 총합이 같아
"순서가 점수에 반영되지 않는" 버그**가 있었다.

**선택 ①(AI 모드).** 비용을 `이동시간 + Σ(importance × 방문순번 × 가중치)`로 바꿨다.
- `importance = 6 − priority` 로 방향을 뒤집는다(priority 1=가장 중요 → importance 5).
- **방문 순번(position)을 곱한다** → 중요한 장소를 늦게 방문할수록 페널티가 커진다. 순서 자체가 점수에 반영된다.

**선택 ②(우선순위 모드).** 단순 penalty가 아니라 **계층적(DP) 최적화**로 구현.
- 우선순위 그룹(1~5)을 **절대 순서로 강제**하고, 같은 그룹 안에서만 이동시간을 최소화한다.
- `dp[(bucket, end_node)] = (cost, path)`로 그룹을 순서대로 확장하며 정확해를 구한다.
- 이로써 원본 문서 11장의 "권장 최종 설계(1순위 우선순위, 2순위 이동시간)"를 **실제 코드로** 구현했다.

**왜.** 기능 이름("내 우선순위")과 알고리즘의 목적이 일치해야 한다. 사용자가 직접 지정한 중요도를
시스템이 이동시간 때문에 임의로 뒤집으면 안 된다.

## B-3. 시간 제약 스케줄링  · PR #4

**고민.** 원본 로드맵의 "특정 시간까지 도착해야 하는 장소"(time window)를 실제로 지원하고 싶었다.

**선택.** 장소별 **약속시각(appointment_time)**·**체류시간(duration_min)**과 **출발 예정 시각(start_time)**을 도입.
- `build_schedule`가 순서를 따라 시간축을 계산: 도착 = 직전 출발 + 이동시간. 일찍 도착하면 약속시각까지 대기, 늦으면 위반으로 기록.
- 위반 순서에는 `APPOINTMENT_VIOLATION_PENALTY_SEC = 10,000,000`(사실상 하드 제약)를 얹어, 약속을 지키는 순서가 항상 우선 선택되게 함.
- `/api/route-eta`가 각 정거장의 도착·출발 시각과 위반 목록을 반환, 프론트가 표시.

**왜.** "가장 효율적인 순서"만이 아니라 "약속을 지킬 수 있는 순서"가 실제 사용자 요구다.

## B-4. LLM 약속시각 자동추출 + UX  · PR #5

**고민.** PR #4까지는 "3시 미팅"의 약속시각을 **사용자가 수동 입력**해야 했다. 이걸 LLM이 산문에서 자동으로 뽑게 하고 싶었다.

**선택 · 데이터 전략.** 기존 학습데이터 200개에는 `appointment_time` 라벨이 없었고, 그중 21개는
input에 시각이 있는데 라벨이 없어 그대로 쓰면 "시각을 무시하도록" 학습된다. 그래서:
- 시각 없는 179개 → `appointment_time: null`로 재활용(정상 negative)
- 시각 있는 21개 → 폐기
- `gemini-3.5-flash-lite`(운영 모델 쿼터 절약용)로 **시각 포함 예시를 보충** → 총 **375개**
- 목표 비율을 정해 튜닝: 문장 기준 **약 40%(실제 39%)**, 장소 기준 17%가 약속시각 보유.
  - 근거: positive가 너무 적으면 추출을 못 배우고, 너무 많으면 시각이 없는데도 아무 시각이나 찍는 과잉추출(hallucination)로 기운다.

**선택 · 정규화.** 모델 출력 시각을 `parse_hhmm`/`format_hhmm`으로 `HH:MM`(24시간제)로 정규화하고,
"오후 3시→15:00", "10시 반→10:30"처럼 오전/오후·반시를 변환. 형식 오류는 버린다.

**선택 · 프론트 UX.**
- 추출된 약속시각을 입력란에 **자동 채움** + **"✨ AI 자동입력" 뱃지**(사용자가 수정하면 뱃지 내림).
- 약속시각 입력을 출발시각과 동일한 **오전/오후·시·분 드롭다운**으로 통일.

**어려웠던 점.** 재파인튜닝은 GPU가 필요해 Colab에서만 가능. Colab 노트북의 GGUF 다운로드 셀 경로 버그
(`routemate_parser/` → 실제 `routemate_parser_gguf/`)와, Unsloth가 자동 생성하는 채팅형 Modelfile을
쓰면 raw passthrough 학습과 어긋나 성능이 떨어지는 함정을 피해야 했다(프로젝트 Modelfile 사용).

## B-5. 경로 계산 전수 검증 + 이동수단 재최적화 버그 발견·수정

**고민.** "정말 세 모드·세 이동수단이 제대로 계산되나?"를 눈으로만 확인하면 놓치는 게 생긴다.

**선택.** UI가 호출하는 백엔드 API를 그대로 구동하는 **검증 하네스**(`docs/verification/verify_routes.py`)를 작성.
10개 시나리오 × 3 이동수단 × 3 최적화모드 = **90건**을 계산하고, 정합성 **불변식**을 자동 검사:
- (A) 최단모드가 실제로 최소 이동시간인지, (B) 우선순위 모드가 우선순위 순서를 지키는지,
- (C) 구간 합이 총합과 맞는지, (D) 도보가 자동차보다 느린지, (E) 약속 위반이 0인지.
결과: **168/168 통과**. 리포트는 `docs/verification/verification-report.md`.

**발견한 버그(중요).** UI로 대조하다가, **이동수단을 바꾸면 방문 순서를 재최적화하지 않고
이전 이동수단의 순서에 ETA만 다시 계산**하는 프론트 버그를 찾았다.
- 증상: 잠실역 도보/AI가 UI에선 1.5km(자동차 순서), 실제 도보 최적은 1.0km.
- 원인: `handleTravelModeChange`가 `optimize-route`를 다시 안 부르고 `fetchRealEta`만 호출. 게다가
  `calculateRoute` 캐시 키가 `mode`뿐이라 재클릭해도 stale 순서를 반환.
- 수정: 이동수단 변경 시 캐시를 비우고 활성 모드를 **새 이동수단으로 재최적화**(`calculateRoute`에 `travelModeArg` 추가해 비동기 `setTravelMode` 이전 값 문제 회피).

**왜 기록하나.** "직접 만든 검증 과정에서 스스로 버그를 찾아 고쳤다"는 것 자체가 좋은 포트폴리오 스토리다.

## B-6. 자동차 실제 도로 경로 지도 표시  · `feature/car-real-route`

**고민.** 지도 경로가 방문 지점을 **직선**으로만 이어, 실제 도로/인도를 반영하지 못했다.

**선택.** 자동차는 Kakao Mobility 응답에 **이미 도로 좌표열(`sections[].roads[].vertexes`)이 들어온다**.
지금까지 summary(거리·시간)만 쓰고 버렸는데, 이 좌표열을 `[[lat,lng],…]`로 추출(`extract_car_path`)해
`route-eta`의 각 leg에 `path`로 실어 보낸다. 프론트는 path가 있으면 실제 경로 Polyline, 없으면(도보/대중교통) 직선 폴백.

**왜.** 자동차 거리·시간은 이미 정확했으므로, "공짜에 가까운" 개선으로 시각적 완성도를 크게 올릴 수 있었다.
행렬 계산 경로에는 `include_path=False`로 좌표 파싱을 생략해 성능 영향도 없앴다.

**남은 것.** 도보(인도)·대중교통 실제 경로는 별도 API(Tmap 보행자/대중교통, ODsay 등)가 필요해 다음 과제로 둔다.

## B-7. 출발시각 '현재시간 기준' 체크박스 + 시간계산 생략

**고민.** 출발시각을 안 고르면 **자동으로 현재 PC 시각을 강제**하던 로직 때문에, 저녁에 오후 약속을
넣으면 "이미 지난 시각"이라 **지각으로 잘못 표시**됐다. 안 고른 것뿐인데 시각이 몰래 적용되는 게 혼란스럽다.

**선택.** 현재시각 적용을 **명시적 선택(체크박스)**으로 바꿨다. 상태는 셋:
- ① 체크 → 현재 PC 시각 기준(드롭다운 비활성화)
- ② 미체크 + 출발시각 선택 → 그 시각
- ③ 미체크 + 미선택 → `start_time=null`(도착시각/지각 **계산 안 함**), 단 약속시각이 있으면 **시간순 정렬은 유지**

**③의 "시간순 정렬"(선택 이유).** 출발시각을 몰라 도착 계산은 못 해도, 약속시각을 굳이 입력했다면
"이른 약속 → 늦은 약속" 순서는 의미가 있다. 그래서 백엔드에 `chronological_penalty_sec`를 추가해,
약속시각 있는 장소들끼리의 역순 쌍(inversion)에만 페널티를 준다(약속 0~1개면 무영향).

**왜.** "모르는 걸 아는 척하지 않는다" — 시스템이 임의로 현재시각을 밀어넣기보다, 사용자가 정보를 줄 때만 시간 계산을 한다.

## B-8. 출발시각 역산 — "몇 시에 나가야 하나?"  · `feature/car-real-route`

**고민.** B-7에서 ③(출발시각 미선택)일 때는 시간 계산을 안 하기로 했다. 하지만 약속시각을 입력했다면,
"그 약속을 지키려면 **몇 시에 나가야 하는지**"를 알려주는 게 진짜 도움이 된다. 이게 사용자와 합의한 2단계.

**선택 · 알고리즘.** 확정된 방문 순서에서 **약속을 모두 지킬 수 있는 '가장 늦은 출발 시각' D\***를 계산한다.
- 핵심 관찰: 출발이 늦어질수록 도착도 늦거나 같아지고(약속 대기는 지연만 시킴), 따라서 **약속 위반 개수는
  출발 시각에 대해 단조 증가**한다.
- 그래서 "위반이 D=0에서의 최소치와 같은 최대 D"를 **이진탐색**으로 찾는다(`recommended_departure`).
  - 모든 약속을 지킬 수 있으면(최소 위반 0) → 그 시각(`feasible=True`)이 "이때까지 나가면 안전"의 마지노선.
  - 불가능하면(약속들이 서로 너무 빡빡) → 위반을 최소로 유지하는 가장 늦은 출발 시각(`feasible=False`).

**선택 · 구조.** `route-eta`를 정리해, leg 이동시간으로 `time_matrix`를 만든 뒤 기존 `build_schedule`로
시간축을 계산하도록 통일했다(도착/출발/종료·위반 계산의 중복 로직 제거). ③+약속이면 D\*를 유효 출발시각으로
써서 스케줄까지 만들어 `recommended_start_time`과 함께 반환한다.

**선택 · UX.** 결과 패널에 "🕒 추천 출발시각 13:50 → 종료 17:30"과 안내를 보여주고,
**"이 시각으로 설정"** 버튼으로 그 시각을 출발시각 드롭다운에 채워 확정할 수 있게 했다(③ → ②).

**어려웠던 점 · 한계.** 약속이 여러 개면 **가장 빡빡한(binding) 약속 하나**가 D\*를 결정하고, 나머지는
일찍 도착해 대기하거나(여유) 못 지킬 수도 있다(빡빡). "모든 약속을 항상 완벽히 만족하는 출발시각"이
존재하지 않을 수 있으므로, 그 경우 위반 최소 기준으로 best-effort 추천하고 이를 UI로 알린다.

*예: 서울역 출발, 광화문(14:00)·명동(17:00) → 추천 13:50(광화문 14:00 정각 도착이 binding, 명동은 일찍 도착해 대기).*

---

# Part C. 배운 것 · 면접 스토리 · 로드맵

## C-1. 개발 관점의 변화
- 기능을 추가하기 전에 그 기능의 **목적함수**를 먼저 정의해야 한다.
- AI를 쓴다고 AI의 모든 출력을 신뢰하면 안 된다(좌표는 지도 API로 검증).
- 외부 API는 성공뿐 아니라 **실패·fallback**까지 설계해야 한다.
- 여러 상태가 얽힌 React 화면은 데이터의 **생명주기와 무효화 조건**을 명확히 해야 한다.
- 사용자가 지정한 값은 시스템이 임의로 재해석하지 않는다(기능 이름 = 알고리즘 목적).
- **직접 검증 하네스를 만들면** 눈으로 못 보던 버그(이동수단 재최적화 누락)를 잡을 수 있다.

## C-2. 면접에서 쓸 핵심 스토리
- **Q. 가장 어려웠던 문제는?** 경로 최적화 자체보다 *서로 다른 목적의 경로를 어떻게 분리 설계할지*였다.
  초기엔 세 모드가 비슷한 비용으로 계산돼 결과가 같아졌다. 각 모드의 목적을 명확히 정의하고,
  우선순위 모드는 계층적 최적화로, AI 모드는 순번 가중 페널티로 실제로 다르게 만들었다.
- **Q. 왜 생성형 AI 결과를 바로 안 쓰고 Kakao로 검증?** 모델은 의미(장소명)를 뽑지만 실제 좌표를 보장하지 않는다. 그래서 의미 분석은 LLM, 지도 데이터는 Kakao로 역할을 분리했다.
- **Q. 왜 Gemini에서 로컬 모델로?** 비용·쿼터·외부 의존을 없애고 오프라인 데모가 가능하도록, 좁은 태스크를 소형 모델 파인튜닝으로 대체했다.
- **Q. 경로 결과가 같은데 버그 아닌가?** 방문 순서가 같으면 거리·시간이 같은 게 정상. 그래서 *먼저 순서를 비교하고*, 순서가 다른데 수치가 같을 때 ETA/캐시를 의심하도록 디버깅 기준을 세웠다.

## C-3. 앞으로의 로드맵
- [x] 내 우선순위: 계층적 최적화로 우선순위 최우선 보장 — **완료(B-2)**
- [x] 시간 약속: time window 제약 모델링 — **완료(B-3)** / LLM 자동추출 — **완료(B-4)**
- [x] AI 추천: 우선순위와 구별되는 별도 점수 — **부분 완료(B-2, 순번 가중)**. 향후 일정 의미·시간제약을 더 반영 가능
- [x] **출발시각 역산**: 약속시각이 있으면 "몇 시에 나가야 하는지"를 역산해 추천 출발시각 표시 — **완료(B-8)**
- [ ] 도보/대중교통 **실제 경로 API**(Tmap 보행자·대중교통/ODsay) 연동 — 현재는 보정값(estimated) ← **다음 작업(B안)**
- [ ] 경로별 총시간·총거리·우선순위 만족도 동시 표시, 지도 구간 정보 강화
- [ ] 성능: 장소 쌍 이동시간 조회 병렬화·캐싱
- [ ] 테스트: 우선순위 역전·동일 좌표·API 실패·후보 변경 등 케이스 확대(검증 하네스 기반)

---

*문서 관리: 기능이 바뀌면 이 파일을 함께 수정하고, 상단 "0. 업데이트 이력"에 한 줄 추가한다.*
*포트폴리오 제출용 Word가 필요하면 이 Markdown에서 변환해 내보낸다.*
