# WTGMate 개발 기록

코드와 함께 git으로 관리하는 개발 기록이다. 기능을 만들 때마다 무엇을 고민했고 왜 그렇게 정했는지, 어떤 문제를 만나 어떻게 풀었는지를 남긴다. 처음 설계는 Part 0에, 이후 바꿔 나간 과정은 Part B에 정리한다.

- 프로젝트: WTGMate. 자연어 일정에서 장소를 뽑아 확인하고, 경로를 최적화·비교하는 웹앱.
- 스택: React + Vite(프론트), FastAPI + Python(백엔드), OR-Tools, Kakao Maps·Mobility·Local, 로컬 파인튜닝 LLM(Ollama).
- 저장소: github.com/ysb2152/WTGMate (프론트/백엔드 모노 구조).

---

## 0. 업데이트 이력 (최신순)

| 시점 | 구분 | 한 일 | 관련 |
|---|---|---|---|
| 2026-08-27 | deploy | 실제 라이브 배포 성공(https://wtg-mate.vercel.app). Vercel 프론트 + Cloudflare Tunnel로 개발 PC의 백엔드/Ollama 노출. 지도·추출·약속·지오코딩·경로 라이브 확인 | B-11 |
| 2026-08-27 | test | solver·스케줄·캐시 단위 테스트 추가 + CI(백엔드 테스트/프론트 빌드) | PR #20 |
| 2026-08-27 | deploy | AWS 배포 산출물 완비(Dockerfile·docker-compose·nginx·Modelfile) + Docker 빌드 CI. Cloudflare Tunnel + Vercel 런북 | B-11 |
| 2026-08-27 | chore | 배포 준비 코드화: requirements.txt, 프론트 API주소·백엔드 CORS를 env로, .env.example 추가 | PR #12 |
| 2026-08-27 | fix | Kakao 자동차 API가 실패해도 200으로 result_code만 주는 짧은 구간에서 summary KeyError 나던 것을 result_code 확인으로 정리 | PR #11 |
| 2026-08-27 | perf/test | 배포용 정확도·성능 측정(최적화 최적성 통과, LLM 추출 6/6, 추정행렬 regret 평균 0.5분) 후 OR-Tools time_limit 5s를 2s로 | PR #10 |
| 2026-08-27 | feat | 대중교통 실제 경로(ODsay `searchPubTransPathT`) 연동, 키 없으면 추정 폴백 | B-10 |
| 2026-08-27 | perf | 실 경로 API 호출을 O(n²)에서 n-1로 축소(순서 최적화는 추정, 최종 경로만 실측) + leg 캐시 | B-9 |
| 2026-08-27 | feat | 도보 실제 인도 경로(Tmap 보행자 경로안내 API) 연동, 키 없으면 추정 폴백 | B-9 |
| 2026-08-26 | fix/test | `clamp_priority(0)`이 falsy로 3이 되던 버그 수정 + 헬퍼 유닛테스트 12개 | PR #7 |
| 2026-08-26 | feat | 출발시각 미입력 시 약속 기준 추천 출발시각 역산 | B-8 |
| 2026-08-26 | feat | 자동차 실제 도로 경로를 지도에 표시(Kakao Mobility 좌표열) | B-6 |
| 2026-08-26 | feat | 출발시각 현재시간 기준 체크박스 + 미선택 시 시간계산 생략 | B-7 |
| 2026-08-26 | test | 경로 계산 전수 검증 하네스 + 이동수단 재최적화 버그 발견·수정 | B-5 |
| 2026-08-26 | feat | LLM 약속시각 자동추출(재파인튜닝) + 프론트 UX | B-4 |
| ~2026-08-25 | feat | 시간 제약 스케줄링(출발시각·체류시간·약속시각·도착시각·위반경고) | B-3 |
| ~2026-08-25 | refactor | Gemini에서 로컬 파인튜닝 모델(Ollama)로 전환 + 세 경로 모드 목적함수 차별화 | B-1, B-2 |
| 이전 | init | 초기 구현 및 설계 초안 | Part 0 |

초안 대비 크게 바꾼 것은 세 가지다. 모델을 Gemini에서 로컬 파인튜닝 모델로 교체했고, AI 추천과 우선순위가 사실상 같은 결과를 내던 문제를 실제로 해결했으며, 약속 시각 같은 시간 제약을 구현했다.

---

# Part 0. 프로젝트의 출발점

처음 이 프로젝트를 구상했을 때의 설계와 계획이다. 이후의 변화는 모두 여기에서 출발했다. 이 문서의 목적이 무엇을 왜 바꿨는가를 남기는 것이기 때문에, 기준점으로 최초 설계를 그대로 남겨 둔다.

**처음의 목표와 형태.** 문장으로 적은 하루 일정에서 장소와 우선순위를 뽑아 지도에 표시하고, 우선순위를 반영한 추천 경로와 예상 소요 시간을 계산하는, 하루치 일정 범위의 MVP를 목표로 했다. 처음에는 React를 그대로 재사용하려고 Electron 데스크톱 앱으로 만들 계획이었지만, 이후 웹(React + Vite)으로 방향을 정했다.

**처음의 기술 선택.** 백엔드는 FastAPI, 프론트는 React와 카카오맵 JS SDK로 잡았고 이 부분은 지금까지 유지하고 있다. 장소 추출은 처음에 Gemini API(`gemini-2.5-flash`)로 문장을 JSON 배열(name/task/priority/lat/lng/address)로 바꾸는 방식이었는데, 비용과 쿼터, 외부 의존 때문에 나중에 로컬 파인튜닝 모델로 교체했다(B-1). 지도와 경로는 카카오를 썼고, 자동차 길찾기는 카카오모빌리티 Directions를 REST 키만으로 사용했다. 도보와 대중교통은 처음에 실제 API 엔드포인트를 확정하지 못해, 직선거리에 도로 굴곡 보정(1.2~1.4배)과 이동수단별 평균 속도를 적용한 근사치(`fallback_leg`)로 대체했다. 도보는 이후 Tmap 실 API로 바꿨다(B-9). 경로 최적화는 하루치라 장소가 보통 10개 미만이어서 완전탐색(순열)을 기본으로 쓰고, 목적지가 `MAX_BRUTE_FORCE_STOPS`(9)를 넘으면 OR-Tools 근사로 자동 전환하도록 했다.

**처음부터 명확히 하려던 원칙.** 세 최적화 모드가 서로 다른 로직으로 확실히 분리되어야 한다는 것이 초기부터의 요구였다. 사실 처음에는 세 모드가 전부 같은 결과를 내는 문제가 있었는데, 우선순위 페널티가 방문 순서와 무관하게 늘 같은 값으로 더해지던 설계 오류였다. 이 문제는 B-2에서 실제로 해결했다.

**처음부터 지킨 방어적 설계.** 키는 `.env`에 넣고 `.gitignore`에 등록했으며, REST 키는 프론트에 노출하지 않고 항상 백엔드에서만 호출했다. Gemini가 실패(429 등)하면 Mock 데이터로 화면이 멈추지 않게 처리하고 `is_mock`으로 알렸는데, Ollama로 바꾼 뒤에는 외부 쿼터 의존 자체가 없어졌다(B-1). 같은 방문 순서를 다른 시점에 두 번 부르면 실시간 교통 때문에 숫자가 미세하게 달라지는 문제는 순서 기반 캐시 키로 막았고, 이 부분은 이후 이동수단 재최적화 버그(B-5)와 최종 경로 leg 캐시(B-9)로 발전했다.

**당시의 한계.** 도보·대중교통 실 API 미연동, 하루치 일정만 다루는 범위, 드래그 방식의 우선순위 UI가 처음의 알려진 한계였다.

---

# Part A. 프로젝트 개요와 현재 설계

**풀려는 문제.** 일정을 입력한 뒤 방문 순서를 사용자가 직접 고민해야 하는 불편함, 가장 가까운 곳만 잇는 경로가 일정의 중요도를 반영하지 못하는 점, AI가 뽑은 장소명이 실제 지도상의 장소와 다를 수 있는 점, 세 종류의 경로를 서로 비교하기 어려운 점, 경로가 바뀌었는데 거리와 시간이 이전 결과와 섞이는 상태 관리 문제를 풀고자 했다.

**사용자 흐름.** 출발지를 검색해 고르고, 이동수단을 선택하고, 자연어로 일정을 입력하면, 로컬 LLM이 장소명과 할 일, 우선순위, 약속 시각을 뽑는다. 그다음 Kakao 장소 검색으로 실제 후보를 확인하고, 사용자가 장소를 확인·수정한 뒤, AI 추천·최단시간·내 우선순위 경로를 각각 계산한다. 거리와 시간은 자동차는 Kakao Mobility로, 도보·대중교통은 Tmap·ODsay로 산출하고, 지도와 방문 순서를 함께 보여 준다.

**세 모드의 목적함수.** 초기 구현에서 가장 큰 문제는 세 경로가 모두 이동시간 중심이라 결과가 비슷해지는 것이었다. 버튼 이름만 다르게 두는 것이 아니라 각 모드가 실제로 다른 목적함수를 가져야 한다고 정리했다.

| 모드 | 최우선 기준 | 의도 |
|---|---|---|
| 최단시간(shortest) | 이동시간 | 우선순위를 무시하고 이동시간만 최소화 |
| 내 우선순위(priority) | 사용자 우선순위 | 중요한 장소를 반드시 앞쪽에(계층적 최적화) |
| AI 추천(ai) | 우선순위 × 방문순번 + 이동시간 | 일정의 의미와 이동 효율의 균형 |

**장소 추출과 검증의 분리.** 자연어를 바로 좌표로 쓰는 것은 위험하다고 판단했다. "강남역"이라고 해도 모델이 준 좌표가 정확하다는 보장이 없기 때문이다. 그래서 의미(장소명·할 일·중요도·약속시각) 추출은 로컬 LLM이 맡고, 실제 좌표 확정은 Kakao Local이 맡도록 역할을 나눴다.

**실패를 고려한 설계.** LLM이 실패하면 Mock 데이터로 화면이 멈추지 않게 하고, Kakao Mobility가 실패하면 Haversine 거리에 이동수단별 보정 속도를 적용한 예상값으로 대체하며 추정치임을 표시한다. 좌표 유효성은 백엔드에서 검증하고 오류가 나면 사용자에게 알리며 로딩을 해제한다.

**프론트 상태 관리.** 경로 결과를 모드별로 따로 저장해 이전 결과가 남거나 덮어써지는 문제를 막았다. 일정을 바꾸면 세 결과를 모두 무효화하고, 우선순위만 바꾸면 priority 결과만 무효화하며, 같은 방문 순서와 같은 이동수단이면 ETA를 다시 부르지 않는다.

---

# Part B. 개발 진행 로그

여기부터가 실제로 손을 대며 프로그램을 발전시킨 기록이다. 각 항목은 무엇이 문제였고 어떤 선택을 했으며 왜 그렇게 했는지, 무엇이 어려웠는지를 남긴다.

## B-1. Gemini에서 로컬 파인튜닝 모델(Ollama)로 전환

처음에는 장소 추출을 Gemini API에 의존했다. 하지만 외부 API는 비용과 쿼터, 네트워크 의존이 있어 데모를 상시 띄우거나 오프라인에서 돌리기 어려웠다. 그래서 소형 오픈 모델인 Qwen2.5-3B-Instruct를 QLoRA로 파인튜닝하고, GGUF(q4_k_m)로 변환해 Ollama에 `wtgmate-parser`로 등록해 서빙하기로 했다. Gemini는 버리지 않고 학습 데이터 생성용 교사 모델로만 남겼다.

이렇게 한 이유는 추론 비용이 들지 않고 오프라인에서 동작하며, 태스크가 장소 추출 JSON으로 좁아서 소형 모델을 파인튜닝해도 충분하다고 판단했기 때문이다. 좌표는 여전히 모델을 신뢰하지 않고, 모델은 name/task/priority만 뽑게 한 뒤 좌표와 주소는 항상 Kakao로 다시 조회해 덮어쓴다. 특히 소형 모델은 좌표를 거의 항상 틀리기 때문이다. 학습은 채팅 형식이 아니라 alpaca 프롬프트(순수 텍스트 이어쓰기)로 했고, 추론도 `raw=True`로 같은 형식을 맞췄다.

AI 모드의 우선순위 지배력 상수 `AI_PRIORITY_WEIGHT_SEC`는 300에서 100으로 조정했다. 실측으로 값을 바꿔 보니 60보다 작으면 우선순위가 거의 반영되지 않고, 100을 넘으면 사실상 priority 모드와 같아졌다. 그 사이 60에서 100이 중요한 곳을 먼저 가되 나머지는 이동시간으로 최적화하는 균형 구간이었다.

어려웠던 점은 학습과 추론의 프롬프트 형식이 조금만 어긋나도 성능이 크게 떨어진다는 것이었다. `TASK_INSTRUCTION`을 백엔드와 데이터 생성 스크립트에서 byte 단위로 동일하게 유지해야 했다. GGUF 가중치가 1.9GB라 git에서 제외하고 Colab 노트북으로 재생성하는 체계도 만들어야 했다. Ollama는 서버 기동 직후 첫 요청이 모델 로딩 때문에 실패해 Mock으로 폴백될 수 있는데, 예열 후에는 정상 동작한다.

## B-2. AI 추천과 우선순위가 같은 결과를 내던 문제 해결

원본 문서가 스스로 지적한 약점이 있었다. ai와 priority가 거의 같은 비용식이라 방문 순서가 같아지는 문제였다. 게다가 우선순위를 이동시간에 그냥 더하기만 하면 순서를 바꿔도 총합이 같아, 순서가 점수에 반영되지 않는 버그가 있었다.

AI 모드는 비용을 `이동시간 + Σ(importance × 방문순번 × 가중치)`로 바꿨다. `importance = 6 − priority`로 방향을 뒤집어 priority 1이 가장 중요하도록 하고, 방문 순번을 곱해서 중요한 장소를 늦게 방문할수록 페널티가 커지게 했다. 순서 자체가 점수에 반영되도록 만든 것이다.

우선순위 모드는 단순 페널티가 아니라 계층적 최적화로 구현했다. 우선순위 그룹을 절대 순서로 강제하고 같은 그룹 안에서만 이동시간을 최소화하며, `dp[(bucket, end_node)] = (cost, path)`로 그룹을 순서대로 확장해 정확해를 구한다. 이렇게 한 이유는 기능 이름(내 우선순위)과 알고리즘의 목적이 일치해야 한다고 생각했기 때문이다. 사용자가 직접 지정한 중요도를 시스템이 이동시간 때문에 임의로 뒤집으면 안 된다고 판단했다.

## B-3. 시간 제약 스케줄링

특정 시간까지 도착해야 하는 장소를 실제로 지원하고 싶었다. 그래서 장소별 약속시각(appointment_time)과 체류시간(duration_min), 출발 예정 시각(start_time)을 도입했다. `build_schedule`이 순서를 따라 시간축을 계산해서, 도착 시각은 직전 출발에 이동시간을 더해 구하고, 일찍 도착하면 약속시각까지 대기하며, 늦으면 위반으로 기록한다. 위반한 순서에는 `APPOINTMENT_VIOLATION_PENALTY_SEC = 10,000,000`을 얹어 사실상 하드 제약으로 두어, 약속을 지키는 순서가 항상 먼저 선택되게 했다. `/api/route-eta`가 각 정거장의 도착·출발 시각과 위반 목록을 반환하고 프론트가 표시한다. 가장 효율적인 순서만이 아니라 약속을 지킬 수 있는 순서가 실제 사용자 요구라고 봤기 때문이다.

## B-4. LLM 약속시각 자동추출

이전까지는 "3시 미팅"의 약속시각을 사용자가 직접 입력해야 했다. 이것을 LLM이 문장에서 자동으로 뽑게 하고 싶었다.

문제는 데이터였다. 기존 학습데이터 200개에는 `appointment_time` 라벨이 없었고, 그중 21개는 입력에 시각이 있는데 라벨이 없어 그대로 쓰면 시각을 무시하도록 학습될 상황이었다. 그래서 시각이 없는 179개는 `appointment_time: null`로 재활용하고, 시각이 있는 21개는 폐기했으며, `gemini-3.5-flash-lite`로 시각 포함 예시를 보충해 총 375개로 늘렸다. 비율도 정해서 튜닝했는데, 문장 기준으로 약 40%, 장소 기준으로 17%가 약속시각을 갖도록 했다. positive가 너무 적으면 추출을 배우지 못하고, 너무 많으면 시각이 없는데도 아무 시각이나 찍는 과잉추출로 기울기 때문이다.

모델이 출력한 시각은 `parse_hhmm`과 `format_hhmm`으로 24시간제 `HH:MM`으로 정규화하고, "오후 3시"를 15:00으로, "10시 반"을 10:30으로 바꾸며 형식 오류는 버린다. 프론트에서는 추출된 약속시각을 입력란에 자동으로 채우고 AI 자동입력 뱃지를 붙였으며(사용자가 수정하면 뱃지를 내림), 약속시각 입력을 출발시각과 같은 오전/오후·시·분 드롭다운으로 통일했다.

어려웠던 점은 재파인튜닝이 GPU가 필요해 Colab에서만 가능했다는 것이다. Colab 노트북의 GGUF 다운로드 셀 경로 버그와, Unsloth가 자동 생성하는 채팅형 Modelfile을 쓰면 raw passthrough 학습과 어긋나 성능이 떨어지는 함정을 피해야 했다. 프로젝트 자체의 Modelfile을 써서 해결했다.

## B-5. 경로 계산 전수 검증과 이동수단 재최적화 버그

세 모드와 세 이동수단이 제대로 계산되는지를 눈으로만 확인하면 놓치는 것이 생긴다고 생각했다. 그래서 UI가 호출하는 백엔드 API를 그대로 구동하는 검증 하네스(`docs/verification/verify_routes.py`)를 만들었다. 10개 시나리오와 3 이동수단, 3 최적화 모드를 조합해 90건을 계산하고, 최단모드가 실제로 최소 이동시간인지, 우선순위 모드가 순서를 지키는지, 구간 합이 총합과 맞는지, 도보가 자동차보다 느린지, 약속 위반이 0인지 같은 불변식을 자동 검사했다. 결과는 168개 검사 전부 통과였고 리포트는 `docs/verification/verification-report.md`에 남겼다.

검증하며 UI로 대조하다가 중요한 버그를 찾았다. 이동수단을 바꾸면 방문 순서를 재최적화하지 않고 이전 이동수단의 순서에 ETA만 다시 계산하는 프론트 버그였다. 잠실역 도보/AI가 UI에서는 자동차 순서 기준으로 1.5km인데 실제 도보 최적은 1.0km였다. 원인은 `handleTravelModeChange`가 `optimize-route`를 다시 부르지 않고 `fetchRealEta`만 호출했고, `calculateRoute` 캐시 키가 `mode`뿐이라 다시 눌러도 이전 순서를 반환한 것이었다. 이동수단을 바꿀 때 캐시를 비우고 새 이동수단으로 재최적화하도록 고쳤다. 직접 만든 검증 과정에서 스스로 버그를 찾아 고쳤다는 점에서 기록해 둘 만하다고 생각했다.

## B-6. 자동차 실제 도로 경로 표시

지도 경로가 방문 지점을 직선으로만 이어서 실제 도로를 반영하지 못했다. 자동차는 Kakao Mobility 응답에 이미 도로 좌표열(`sections[].roads[].vertexes`)이 들어오는데, 그동안 summary(거리·시간)만 쓰고 버리고 있었다. 이 좌표열을 `[[lat,lng], …]`로 추출해(`extract_car_path`) `route-eta`의 각 leg에 `path`로 실어 보냈다. 프론트는 path가 있으면 실제 경로 Polyline으로, 없으면 직선으로 그린다. 자동차 거리와 시간은 이미 정확했기 때문에 거의 비용을 들이지 않고 시각적 완성도를 크게 올릴 수 있었다. 행렬 계산 경로에는 `include_path=False`로 좌표 파싱을 생략해 성능 영향도 없앴다. 도보와 대중교통 실제 경로는 별도 API가 필요해 다음 과제로 두었다.

## B-7. 출발시각 현재시간 기준 체크박스

출발시각을 안 고르면 자동으로 현재 PC 시각을 강제하던 로직 때문에, 저녁에 오후 약속을 넣으면 이미 지난 시각이라 지각으로 잘못 표시됐다. 고르지 않은 것뿐인데 시각이 몰래 적용되는 것이 혼란스러웠다. 그래서 현재시각 적용을 명시적 선택(체크박스)으로 바꿨다. 상태는 셋으로, 체크하면 현재 PC 시각을 쓰고, 미체크에 출발시각을 고르면 그 시각을 쓰며, 미체크에 미선택이면 `start_time`을 null로 두어 도착시각과 지각을 계산하지 않는다.

미선택이어도 약속시각을 굳이 입력했다면 이른 약속에서 늦은 약속으로 가는 순서는 의미가 있다고 봤다. 그래서 백엔드에 `chronological_penalty_sec`를 추가해, 약속시각이 있는 장소들끼리의 역순 쌍에만 페널티를 준다(약속이 0~1개면 영향이 없다). 시스템이 임의로 현재시각을 밀어넣기보다 사용자가 정보를 줄 때만 시간 계산을 하도록 한 것이다.

## B-8. 출발시각 역산

B-7에서 출발시각을 안 고르면 시간 계산을 안 하기로 했지만, 약속시각을 입력했다면 그 약속을 지키려면 몇 시에 나가야 하는지를 알려 주는 것이 진짜 도움이 된다고 생각했다. 그래서 확정된 방문 순서에서 약속을 모두 지킬 수 있는 가장 늦은 출발 시각을 계산했다.

핵심 관찰은 출발이 늦어질수록 도착도 늦거나 같아지고(약속 대기는 지연만 시키므로), 따라서 약속 위반 개수가 출발 시각에 대해 단조 증가한다는 것이었다. 그래서 위반이 D=0에서의 최소치와 같아지는 가장 늦은 출발 시각을 이진탐색으로 찾았다(`recommended_departure`). 모든 약속을 지킬 수 있으면 그 시각이 이때까지 나가면 안전한 마지노선이 되고, 지킬 수 없으면 위반을 최소로 유지하는 가장 늦은 시각을 알려 준다.

구조도 정리해서, leg 이동시간으로 `time_matrix`를 만든 뒤 기존 `build_schedule`로 시간축을 계산하도록 통일했다(도착·출발·위반 계산의 중복 로직을 없앴다). 결과 패널에는 추천 출발시각과 종료 시각을 보여 주고, 그 시각을 출발시각 드롭다운에 채워 확정하는 버튼을 뒀다.

한계도 있다. 약속이 여러 개면 가장 빡빡한 약속 하나가 추천 시각을 결정하고 나머지는 일찍 도착해 대기하거나 못 지킬 수도 있다. 모든 약속을 완벽히 만족하는 출발시각이 존재하지 않을 수 있어, 그 경우 위반을 최소로 하는 시각을 추천하고 이를 UI로 알린다. 예를 들어 서울역에서 출발해 광화문(14:00)과 명동(17:00)을 갈 때는 광화문 정각 도착이 기준이 되어 13:50을 추천하고, 명동은 일찍 도착해 대기한다.

## B-9. 도보 실제 경로와 무료 쿼터를 고려한 호출 구조

자동차는 실제 도로를 그리는데 도보와 대중교통은 여전히 직선 추정이었다. 도보만이라도 실제 인도 경로와 거리, 시간을 쓰고 싶어서 Tmap 보행자 경로안내 API를 연동했다. 응답이 GeoJSON이라 첫 Point의 `totalDistance`와 `totalTime`으로 거리·시간을 얻고, LineString 좌표열을 `[[lat,lng], …]`로 뒤집어 지도 경로로 쓴다. 자동차와 같은 leg `path` 구조로 실어서 프론트는 이동수단과 무관하게 그대로 그린다. 키가 없으면 기존 추정으로 안전하게 폴백하고, `estimated` 플래그로 추정치 여부를 UI에 표시한다.

인증에서 헤맸다. 상품 상세 페이지에는 키가 없었고, Tmap은 상품을 구독한 뒤 My Page의 App에 붙는 appKey를 요청 헤더에 넣는 방식이라, 구독하지 않으면 게이트웨이가 `403 INVALID_API_KEY`로 막았다. 서버측 호출이라 도메인 제한이 없다는 점도 Kakao JS 키와 달랐다.

더 큰 고민은 무료 쿼터가 너무 적다는 것이었다. Tmap 대중교통은 하루 10회, 도보도 유한한데, 순서 최적화는 n×n 거리 행렬을 만드느라 실 API를 `n(n-1)`회 부른다. 4곳이면 15회라 대중교통은 데모를 한 번도 완주할 수 없었다. 그래서 실 API의 가치가 어디에 있는지를 다시 봤다. 방문 순서 최적화에는 정확한 실측이 굳이 필요 없고(2~5곳이면 직선 추정만으로도 순서가 실제와 거의 같다), 실측이 진짜 필요한 곳은 지도에 그릴 최종 경로와 사용자에게 보여 줄 정확한 총거리·시간뿐이며, 이 둘은 모두 확정된 최종 경로에서만 쓰인다. 그래서 거리 행렬은 추정치로만 계산하고(`build_time_distance_matrix`가 실 API를 부르지 않음), 실 API는 최종 경로(`route-eta`)의 `n-1`개 구간에만 쓰도록 바꿨다. 그 결과 최적화 한 번당 실 호출이 2곳이면 3회에서 1회로, 3곳이면 8회에서 2회로, 5곳이면 24회에서 4회로 줄었다.

여기에 leg 캐시(`get_final_leg`)를 얹어 같은 좌표쌍과 이동수단의 실측 결과를 재사용했다. 이동수단을 토글하거나 같은 장소를 다시 최적화해도 추가 호출이 없다. 상시 가동을 대비해 크기 상한(LRU 512)을 두고, 실시간성이 있는 자동차와 대중교통에는 TTL(600초)을 뒀다(도보는 거리가 변하지 않아 무기한). 실제 좌표열을 받은 실측만 캐시하고, 실패하거나 키가 없어 폴백된 결과는 캐시하지 않아 이후 실측 기회를 막지 않도록 했다. 정확도가 필요한 지점과 대충 해도 되는 지점을 분리한 트레이드오프다. 데모에서 보이는 경로와 숫자는 실측 그대로 유지하면서, 희소한 무료 쿼터를 태우던 O(n²) 내부 계산만 추정으로 돌린 것이다.

## B-10. 대중교통 실제 경로(ODsay)와 엔드포인트 이름 삽질

남은 마지막 이동수단인 대중교통을 실제 경로로 그리고 싶었다. Tmap 대중교통은 무료 10회/일이라 데모조차 어려워서 ODsay 대중교통 길찾기 API를 골랐다. 응답 `result.path[0]`에서 `info.totalTime`으로 시간을, `subPath[].distance` 합으로 거리를 얻고, 각 subPath의 `passStopList.stations[]`(x=경도, y=위도)를 이어 실제 경로 폴리라인을 만든다. 도보·자동차와 같은 leg `path` 구조로 실어서 프론트는 그대로 그린다. ODsay는 정확한 도로 선형을 얻으려면 `loadLane`를 추가로 불러야 하는데 그러면 leg당 2회가 되므로, 데모에는 역과 정류장 좌표를 잇는 폴리라인으로 충분하다고 보고 `loadLane`를 생략해 leg당 1회로 묶었다. 키가 없거나 실패하면 기존 추정으로 폴백한다.

두 번 헤맸다. 하나는 인증인데, ODsay는 서버 호출용 '서버' 키와 공인 IP 등록이 필요했다. 브라우저용 URI 키는 Referer로 막혀 서버에서는 쓸 수 없어서, 로컬 개발기의 egress IP를 등록해야 했다. 다른 하나는 엔드포인트 이름이었다. 흔히 알려진 `searchPubPathT`로 붙였는데 계속 `code 500 "페이지를 찾을 수 없습니다"`가 떴다. IP와 키, 인코딩을 다 의심했지만, apiKey 없이 부르거나 일부러 틀린 주소로 불러도 같은 메시지가 나오는 것을 보고 이 메시지가 범용 에러라는 것을 알았다. 공식 레퍼런스로 확인하니 정확한 이름은 `searchPubTransPathT`('Trans' 포함)였다. "page not found"가 말 그대로 없는 엔드포인트를 가리킨 것이었다.

이미 만들어 둔 행렬은 추정, 최종 경로만 실측, 캐시 구조(B-9) 덕분에, 무료 30회/일이라는 극한 쿼터에서도 최적화 한 번당 대중교통 호출이 `n-1`회로 유지된다(2곳 1회, 5곳 4회). 새 이동수단을 붙일 때 호출 구조를 다시 손댈 필요가 없었다. 서울역에서 강남역까지 실제로 불러 보니 33분에 15.2km, subPath 5개(도보와 지하철 혼합), 경로 좌표 13점이 나왔고, `route-eta`에서 `estimated=False`와 2차 호출 캐시 히트를 확인했다. ODsay 무료는 30회/일이라 개인 데모용이고, 공개 라이브 데모는 유료 플랜이거나 대중교통을 추정으로 두는 선택이 필요하다.

## B-11. 배포: AWS는 준비만 하고 라이브는 Cloudflare Tunnel로

라이브: https://wtg-mate.vercel.app

실제 접속 가능한 URL로 올리고 싶었지만, 이 앱의 백엔드는 로컬 파인튜닝 3B 모델(Ollama)을 쓴다. 이 모델이 RAM 2~3GB를 요구해 무료나 저가 호스팅에는 들어가지 않고, AWS로 가면 t3.medium 상시 가동으로 월 30달러 정도가 든다.

그래서 두 갈래로 나눴다. 먼저 AWS는 프로비저닝만 하면 바로 뜨는 상태로 완비만 했다. 실제로 과금하며 운영하는 대신 배포 산출물을 전부 갖췄다. `backend/Dockerfile`과 `docker-compose.yml`(ollama, 부팅 시 `ollama create`를 자동으로 하는 model-loader, backend 세 서비스), `deploy/nginx-wtgmate.conf`(정적 프론트와 `/api` 프록시, certbot), 로컬 Ollama에서 복구한 `backend/finetune/Modelfile`을 만들었고, GitHub Actions로 백엔드 이미지를 실제로 빌드해 언제나 빌드 가능하다는 것을 CI로 보증했다.

실제 라이브는 비용이 들지 않는 Cloudflare Tunnel과 Vercel로 운영했다. 백엔드와 Ollama가 이미 개발 PC에서 잘 돌고, 모든 API 키(Kakao·Tmap·ODsay)가 그 PC의 IP로 이미 인증되어 있다는 점을 활용했다. EC2에 모델을 옮기는 대신, 개발 PC의 FastAPI(8000)를 Cloudflare Tunnel로 HTTPS 노출하고 정적 프론트만 Vercel에 올렸다.

```
브라우저 ─https─▶ Vercel(프론트) ─/api─https─▶ Cloudflare Tunnel ─▶ 개발 PC: FastAPI + Ollama
```

배포를 위해 코드도 손봤다. 프론트 API 주소를 `VITE_API_BASE_URL` env로, 백엔드 CORS를 `ALLOWED_ORIGINS` env로 빼고(자격증명을 쓰지 않아 `allow_credentials=False`), `requirements.txt`와 `.env.example`을 추가했다. 이렇게 나누니 AWS 배포를 설계·완비할 수 있다는 것과 실제로 접속되는 데모를 둘 다 얻으면서 상시 과금은 피할 수 있었다. 다만 Cloudflare quick tunnel URL은 재시작마다 바뀌고, 데모는 PC를 켜 둔 동안만 라이브라는 한계가 있다.

배포하며 겪은 함정은 Kakao의 JavaScript SDK 도메인이었다. Vercel에 올린 뒤 프론트와 API는 다 되는데 지도만 404로 뜨지 않았다. 키는 로컬과 같고 백엔드 파싱과 지오코딩도 라이브로 동작하는데 지도만 죽었다. 원인은 Kakao 콘솔에서 일반 Web 플랫폼 사이트 도메인과 별개로 JavaScript SDK 도메인을 따로 등록해야 한다는 점이었다(둘이 다른 설정이다). 후자에 `https://wtg-mate.vercel.app`을 넣으니 해결됐다. Vercel이 `VITE_` 접두 변수를 Secret으로 저장하려다 막는 것도 있었는데, 이 값들은 원래 브라우저에 공개되고 도메인으로 보호되므로 Config 타입으로 저장해야 빌드에 주입된다. 라이브 사이트에서 자연어 일정으로 장소 4개를 추출하고 지도와 마커, 경로, Kakao 후보 검색까지 동작하는 것을 확인했다.

---

# Part C. 배운 것과 로드맵

## C-1. 개발하며 바뀐 관점

기능을 추가하기 전에 그 기능의 목적함수를 먼저 정의해야 한다는 것을 배웠다. AI를 쓴다고 모든 출력을 신뢰하면 안 되고(좌표는 지도 API로 검증했다), 외부 API는 성공뿐 아니라 실패와 폴백까지 설계해야 한다. 여러 상태가 얽힌 React 화면은 데이터의 생명주기와 무효화 조건을 명확히 해야 하고, 사용자가 지정한 값은 시스템이 임의로 재해석하면 안 된다. 직접 검증 하네스를 만들면 눈으로는 못 보던 버그도 잡을 수 있다는 것도 확인했다.

## C-2. 앞으로의 로드맵

- [x] 내 우선순위 계층적 최적화 (B-2)
- [x] 시간 약속 제약 모델링 (B-3) 및 LLM 자동추출 (B-4)
- [x] AI 추천을 우선순위와 구별되는 별도 점수로 (B-2)
- [x] 출발시각 역산 (B-8)
- [x] 도보 실제 경로 API 연동 (B-9)
- [x] 대중교통 실제 경로 API 연동 (B-10)
- [x] 성능·쿼터 최적화(실 API 호출을 O(n²)에서 n-1로 줄이고 leg 캐시 적용) (B-9)
- [x] 배포(AWS 산출물과 Docker CI + 실제 라이브 Cloudflare Tunnel·Vercel) (B-11)
- [x] solver·스케줄·캐시 단위 테스트와 CI
- [ ] 경로별 총시간·총거리·우선순위 만족도 동시 표시, 지도 구간 정보 강화
- [ ] 대중교통 호출량 병렬화

---

문서 관리: 기능이 바뀌면 이 파일도 함께 수정하고, 상단 업데이트 이력에 한 줄 추가한다.
